name: lego_app
description: "A cross-platform LEGO set rebuilding application."
publish_to: "none"
version: 0.1.9

environment:
  sdk: ^3.10.3

dependencies:
  cached_network_image: ^4.0.0
  cached_network_image_web: ^2.0.0
  flutter:
    sdk: flutter
  flutter_hooks: ^0.21.3
  flutter_riverpod: ^3.4.2
  go_router: ^18.0.0
  hooks_riverpod: ^3.4.2
  http: ^1.6.0
  material_3_expressive: ^1.1.1
  material_new_shapes: ^1.0.0
  material_ui: ^1.1.0
  motor: ^1.1.0
  nativeapi: ^0.2.0
  package_info_plus: ^10.2.1
  riverpod_annotation: ^4.0.6
  shared_preferences: ^2.5.4
  supabase_flutter: ^2.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  riverpod_generator: ^4.0.8
  build_runner: ^2.16.0

flutter:
  uses-material-design: true
